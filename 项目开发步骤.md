# OperatorEngine 项目开发步骤

## 项目定位

OperatorEngine 是阶段三、阶段四之后的工程化整合项目，目标不是继续学习更多孤立的 GPU 优化技巧，而是把已有的 Triton/CUDA kernel 组织成一个真正可安装、可调用、可测试、可 benchmark、可扩展的 PyTorch 算子库，后续可以实现在 Pytorch 中安装自己的算子库并使用。

项目完成后，应形成下面这条完整链路：

```text
Python torch.Tensor
-> PyTorch Operator 调度器
-> C++ 参数与设备检查
-> 内部 kernel 选择器
-> Triton / CUDA Core / Tensor Core implementation
-> current CUDA stream
-> output torch.Tensor
-> correctness / opcheck / benchmark
-> Transformer Block 集成
```

本项目是“阶段 4.5”，暂时替代阶段五。当前不学习 NCCL、TP、PP、PD 分离，也不依赖多卡或实验室集群。

## 当前基础

已有代码可作为算法和性能实验参考：

- `../3_Triton/`：Vector Add、Softmax、GEMM、LayerNorm、Naive Attention、FlashAttention 等 Triton 实现和 benchmark。
- `../4_CUDA/`：CUDA Reduction、Softmax、GEMM、Tensor Core、Attention，以及 Nsight Systems/Compute 报告。
- `../4_CUDA/05_cuda_attention/attention_api.h`：已有 Attention C++ 接口原型。
- `../4_CUDA/05_cuda_attention/profiling_summary.md`：已有 Attention 性能结论。

旧目录视为学习实验档案，尽量不要动旧目录的代码，保证其学习路径的完整性。需要复用算法时，将核心 kernel 有选择地迁移到 OperatorEngine 项目中，并重新设计工程接口。不要把包含 `main()`、数据生成和 benchmark 的旧 `.cu` 文件直接编进扩展库。

## 项目边界

### 第一版必须支持

- NVIDIA CUDA GPU。
- PyTorch `torch.Tensor` 输入输出。
- Inference forward。
- Vector Add、Softmax、GEMM、Attention。
- 正确使用 PyTorch 当前 CUDA device 和 current CUDA stream。
- 明确的 shape、dtype、device、layout 检查。
- PyTorch reference correctness test。
- `torch.library.opcheck` 注册契约检查。
- PyTorch / Triton / CUDA benchmark。
- 一个最小 Transformer Block 集成实验。

### 第一版明确不做

- 多 GPU、NCCL、TP、PP、PD 分离。
- Autograd/backward。
- CPU 高性能实现。
- ROCm 或其他非 NVIDIA 后端。
- Ragged/Paged Attention。
- 完整 vLLM/SGLang 接入。
- 所有 dtype、所有 layout、所有 shape 的生产级泛化。
- 以超过 cuBLAS、PyTorch SDPA 为强制目标。

不支持的输入必须清晰报错或由 Python 层显式 fallback，不能静默算错。

## 预期目录结构

目录按里程碑逐步创建，不要第一天生成所有空文件。

```text
OperatorEngine/
  pyproject.toml
  setup.py
  README.md
  my_ops/
    __init__.py
    _meta.py
    functional.py
  csrc/
    register.cpp
    common/
      checks.h
      cuda_utils.cuh
    vector_add/
      vector_add.h
      vector_add_cuda.cu
    softmax/
      softmax.h
      softmax_cuda.cu
    gemm/
      gemm.h
      gemm_cuda.cu
    attention/
      attention.h
      attention_cuda.cu
      attention_dispatch.cpp
  tests/
    conftest.py
    test_vector_add.py
    test_softmax.py
    test_gemm.py
    test_attention.py
    test_transformer_block.py
  benchmarks/
    common.py
    benchmark_vector_add.py
    benchmark_softmax.py
    benchmark_gemm.py
    benchmark_attention.py
    benchmark_transformer_block.py
    results/
  examples/
    transformer_block.py
  docs/
    architecture.md
    dispatch_policy.md
    performance_report.md
    limitations.md
```

## 工程约定

### 构建方式

第一版使用 PyTorch 官方 C++/CUDA Extension 路径：

- `setuptools`
- `torch.utils.cpp_extension.CUDAExtension`
- `torch.utils.cpp_extension.BuildExtension`
- ahead-of-time editable install

第一版不引入独立 CMake。等全部算子完成、确实需要脱离 Python 构建或管理第三方库时，再评估迁移到 CMake/scikit-build。

推荐开发安装命令：

```bash
python -m pip install -e . --no-build-isolation
```

使用 `--no-build-isolation` 是为了让构建过程直接使用当前环境中已经安装并与 CUDA 匹配的 PyTorch。

### 算子命名与注册

统一使用命名空间：

```text
my_ops::vector_add
my_ops::softmax
my_ops::gemm
my_ops::attention
```

C++ 中使用：

```text
TORCH_LIBRARY(my_ops, ...)
TORCH_LIBRARY_IMPL(my_ops, CUDA, ...)
```

Python 中通过以下形式调用底层算子：

```python
torch.ops.my_ops.vector_add(a, b)
```

`my_ops.functional` 可以提供更友好的 Python wrapper 和显式 fallback，但不能掩盖底层算子不支持的输入范围。

### PyTorch 集成规则

- 用 `TORCH_CHECK` 检查输入契约。
- 输出由 `torch::empty_like` 或带明确 options 的 `torch::empty` 分配。
- 不能在 kernel 内部自行创建新的 CUDA context。
- 必须遵循输入 Tensor 所在 device。
- 必须使用 PyTorch current CUDA stream，不能写死 default stream。
- kernel launch 后执行 CUDA launch error check。
- 不在正常执行路径中无条件调用 `cudaDeviceSynchronize()`。
- schema 必须正确描述 mutation 和 aliasing；第一版算子优先设计为 functional operator，返回新 Tensor。
- 如果需要支持 `torch.compile`，在 Python 中注册 FakeTensor/meta implementation，并用 `torch.library.opcheck` 验证。

### 测试规则

- 数值正确性使用 `torch.testing.assert_close`。
- `torch.library.opcheck` 只检查注册契约，不能替代数值测试。
- 每个算子同时测试正常输入、边界输入和非法输入。
- 每个 bug 必须先增加失败测试，再修复实现。
- tolerance 必须根据 dtype、累加精度和问题规模解释，不能为了通过测试随意放宽。

### 性能规则

- benchmark 前必须 warmup。
- GPU kernel 时间优先使用 CUDA Event 或 `torch.utils.benchmark` 的正确同步方式。
- 比较时固定 dtype、shape、layout、stream 和数学语义。
- 报告至少包含 latency、GB/s 或 TFLOPS、speedup、正确性误差和硬件环境。
- 每个优化实验只改变一个主要变量。
- 真实结论必须覆盖多个 shape，不能只展示最有利的单点结果。
- 不把 Nsight Compute replay 后的 Duration 当作真实性能时间。

## Phase 0：冻结环境与写清架构

### 任务

1. 记录以下环境信息到 `docs/architecture.md`：
   - 操作系统/WSL 版本。
   - GPU 型号与 compute capability。
   - NVIDIA driver。
   - CUDA Toolkit / `nvcc`。
   - Python、PyTorch、Triton 版本。
   - 编译器版本。
2. 画出 Python、Dispatcher、C++ wrapper、CUDA kernel 的调用关系。
3. 决定第一版支持矩阵，并写入 README：
   - device：CUDA only。
   - dtype：Vector Add 先 FP32。
   - layout：contiguous only。
   - autograd：not supported。
4. 确认旧 Vector Add kernel 的输入参数、grid/block、mask/边界策略。

### 验收标准

- 环境版本可复现。
- 项目边界清楚。
- 新上下文能根据文档说明为什么先做 Vector Add，而不是直接迁移 Attention。

## Phase  1：Vector Add 工程闭环

这是当前唯一需要开始的里程碑。

### Step 1：创建最小 Python 包

只创建：

```text
pyproject.toml
setup.py
my_ops/__init__.py
csrc/register.cpp
csrc/vector_add/vector_add.h
csrc/vector_add/vector_add_cuda.cu
tests/test_vector_add.py
```

目标是 `import my_ops` 时加载编译好的 `_C` 扩展并触发算子注册。

### Step 2：定义算子契约

第一版 schema：

```text
vector_add(Tensor a, Tensor b) -> Tensor
```

第一版输入约束：

- `a`、`b` 都在 CUDA。
- 两者 device 相同。
- 两者都是 FP32。
- shape 相同。
- contiguous。
- 输出 shape/dtype/device 与输入一致。

### Step 3：编写 C++ wrapper

C++ wrapper 负责：

1. 参数检查。
2. 设置/保护正确 CUDA device。
3. 分配输出 Tensor。
4. 空 Tensor 直接返回，避免启动 0 block kernel。
5. 获取 current CUDA stream。
6. 调用 `.cu` 中的 launcher。
7. 检查 launch error。

C++ wrapper 不负责 benchmark，不生成随机数据，不包含 `main()`。

### Step 4：迁移 CUDA kernel

从旧 Vector Add 实验中迁移最小 kernel：

- grid-stride 或 `ceil_div(numel, threads)` 均可，第一版选择最清楚的一种。
- 正确处理非 2 的幂长度。
- 不在 kernel 中假设输入地址天然适合 `float4`。
- 第一版先使用标量 FP32，向量化放到后续消融实验。

### Step 5：注册 Dispatcher

- `TORCH_LIBRARY` 只定义 schema。
- `TORCH_LIBRARY_IMPL(..., CUDA, ...)` 绑定 CUDA 实现。
- Python import 后确认 `torch.ops.my_ops.vector_add` 存在。

### Step 6：编写测试

至少覆盖：

1. 长度 `1024`。
2. 非 2 的幂长度 `1003`。
3. 二维和三维 Tensor，kernel 内部按 `numel` 处理。
4. 空 Tensor。
5. shape 不一致，必须报错。
6. CPU Tensor，必须报错。
7. dtype 非 FP32，第一版必须明确报错。
8. 非 contiguous Tensor，第一版必须明确报错。
9. 非默认 CUDA stream，结果必须正确且不能发生错误同步。
10. `torch.library.opcheck`。

### Step 7：编写 benchmark

对比：

```text
torch.add
my_ops CUDA scalar
旧 Triton vector_add
```

覆盖小、中、大三类输入。Vector Add 是 memory-bound，重点报告 latency 和有效 GB/s，不要求自定义 kernel 必须超过 `torch.add`。

### Phase 1 验收标准

以下命令必须成功：

```bash
python -m pip install -e . --no-build-isolation
python -m pytest tests/test_vector_add.py -q
```

以下调用必须工作：

```python
import torch
import my_ops

a = torch.randn(1003, device="cuda", dtype=torch.float32)
b = torch.randn_like(a)
out = torch.ops.my_ops.vector_add(a, b)
torch.testing.assert_close(out, a + b)
```

在全部测试通过、benchmark 可复现、README 写清支持范围以前，不进入 Softmax。

## Phase 2：Softmax 与 dtype dispatch

### 目标

把“一个固定 kernel”升级为“根据 dtype 和最后一维 shape 选择实现的工程算子”。

### 任务

1. 定义 `my_ops::softmax(Tensor input, int dim) -> Tensor`，第一版只支持最后一维。
2. 支持 FP32、FP16，FP32 accumulation；BF16 作为后续扩展。
3. 迁移至少两个 CUDA 实现：
   - block/shared-memory reduction。
   - warp-shuffle reduction。
4. 接入已有 Triton Softmax 作为独立 reference/候选实现，不把 Triton JIT 代码塞进 C++ translation unit。
5. 根据列长度、dtype 和资源限制选择实现。
6. 对不支持的列长度明确 fallback 或报错。
7. 测试数值稳定性：大正数、大负数、非 2 的幂列数。
8. benchmark 对比 `torch.softmax`、Triton、两个 CUDA 版本。

### 验收标准

- 能解释 selector 为什么在不同 shape 选择不同 kernel。
- 支持范围、fallback 和误差边界有文档。
- 不能只保留性能最好的 shape。

## Phase 3：GEMM 与配置选择

### 目标

把已有 CUDA GEMM 优化版本变成可调度的库实现，并理解自定义 GEMM 与 cuBLAS 的工程边界。

### 任务

1. 先定义最小二维 GEMM 契约，明确 row-major、transpose 和输出 dtype。
2. 接入：
   - tiled CUDA Core baseline。
   - register blocking/vectorized 版本。
   - Tensor Core 版本。
3. 使用真实 Transformer shape，而不只测试方阵：
   - QKV projection。
   - output projection。
   - MLP up/gate/down projection。
   - Prefill-like 大 M 与 decode-like 小 M。
4. 建立静态 dispatch table：根据 M/N/K、dtype、alignment 选择实现。
5. 不支持的 shape 回退 `torch.matmul`/cuBLAS。
6. 第一版不做复杂 autotuner；先用离线 benchmark 生成配置结论。

### 验收标准

- 对每类 Transformer shape 都有正确性与性能记录。
- 能解释为什么自定义 kernel 在某些 shape 下应该主动 fallback 到 cuBLAS。
- selector 的判断有测试，不依赖隐藏的硬编码假设。

## Phase 4：Attention 泛化与统一调度

### 目标

把现有单个 `N × d` 实验升级为接近真实模型接口的 Attention forward。

### 第一版接口范围

```text
Q, K, V: [B, H, N, D]
output:  [B, H, N, D]
D: 64 或 128
causal: true / false
dtype: FP16，FP32 accumulation
layout: contiguous
```

### 任务

1. 先写 PyTorch reference：scale、mask、softmax、`P @ V`。
2. 迁移 CUDA Core online Attention。
3. 迁移 Tensor Core 路径，并保留明确的 alignment/head_dim 限制。
4. 接入 Triton FlashAttention 作为候选实现。
5. selector 至少考虑：dtype、head_dim、sequence length、causal、alignment、shared-memory 限制。
6. 不支持时显式调用 PyTorch SDPA fallback。
7. correctness 覆盖多组 B/H/N/D、causal、边界长度和随机种子。
8. 对比：PyTorch SDPA、Triton、CUDA Core、Tensor Core。

### 验收标准

- API 能接收真实四维 Attention Tensor。
- 不支持的 shape 不会进入错误 kernel。
- 每个 dispatch 分支都有测试。
- 能说明 kernel speedup 与 wrapper/dispatch/中间转换开销的关系。

## Phase 5：最小 Transformer Block 集成

### 目标

证明自定义算子不只在 microbenchmark 中工作，还能进入真实计算图。

### 任务

1. 用纯 PyTorch 实现最小 decoder-only block：

```text
RMSNorm
-> QKV projection
-> RoPE
-> Attention
-> output projection
-> residual
-> RMSNorm
-> SwiGLU MLP
-> residual
```

2. 保存纯 PyTorch baseline。
3. 逐个替换算子，不要一次全部替换：
   - Attention。
   - Softmax（仅用于 unfused 对照）。
   - GEMM（只替换 selector 明确支持的 shape）。
4. 每替换一个算子，重新做 correctness 和端到端 benchmark。
5. 区分 kernel time 与端到端 block time。
6. 解释为什么局部 `3x` 加速通常不会带来 block 级 `3x` 加速。

### 验收标准

- 相同输入和权重下，输出与 PyTorch baseline 在规定 tolerance 内一致。
- 能给出每次替换前后的端到端数据。
- 能识别 launch、layout conversion、fallback、未优化算子的占比。

## Phase 6：工程收尾

### 必须完成

1. `pip install -e .` 可安装。
2. 一条命令运行全部测试。
3. 一条命令运行核心 benchmark。
4. README 包含：
   - 项目目标。
   - 支持矩阵。
   - 构建方式。
   - 使用示例。
   - 正确性说明。
   - benchmark 环境和结果。
   - 已知限制。
5. `docs/architecture.md` 解释 Dispatcher、selector 和 kernel 的职责边界。
6. `docs/dispatch_policy.md` 记录每条选择规则及证据。
7. `docs/performance_report.md` 包含成功与失败实验，不能只保留最好结果。
8. 清理扩展库中的 benchmark `main()`、重复 CUDA error macro 和散落硬编码。

### 最终完成标准

- 这是一个算子库，而不是若干 `.cu` 示例的集合。
- 新用户可以安装并从 Python 调用。
- 非法输入会得到明确错误或显式 fallback。
- correctness、registration contract 和性能都有自动验证。
- 至少一个 Transformer Block 使用该库并完成端到端对比。
- 能从 Python 调用一直追踪到具体 CUDA/Triton kernel。
- 能解释所有性能结论的适用 shape、硬件和限制。

完成以上标准后，再进入阶段五的单卡推理部分：Prefill/Decode、KV Cache、continuous batching 和 vLLM 调度。多卡内容继续等待真实硬件资源。

## 推荐开发节奏

不要按“看完资料再写代码”的方式推进。每个小步骤遵循：

```text
明确契约
-> 用户先实现最小版本
-> 编译/运行
-> 根据真实错误排查
-> 补测试
-> review 接口与资源生命周期
-> benchmark
-> 写结论
```

一次只解决一个边界。例如 Vector Add 的推荐提交顺序：

```text
1. build: extension 可以编译和 import
2. feat: 注册 vector_add schema 与 CUDA implementation
3. test: 添加数值、边界和非法输入测试
4. fix: 正确使用 current CUDA stream
5. test: 添加 opcheck 与 non-default stream 测试
6. bench: 对比 torch.add 和 Triton
7. docs: 写明支持范围与结论
```

## 新上下文的协作要求

新的 Codex 上下文应遵守以下方式：

1. 首先完整阅读本文件，再检查当前 OperatorEngine 目录和相关旧代码。
2. 每次只推进当前里程碑，不提前实现后续算子。
3. 以教学和 code review 为主：先解释当前文件的职责、接口和关键约束，再让用户完成核心代码；除非用户明确要求代写，不一次性生成整个项目。
4. 遇到编译或运行错误时，先读取真实输出并定位根因，不靠猜测反复改环境。
5. 每次修改后运行与改动相匹配的测试。
6. 不为了“更工程化”引入当前没有价值的抽象。
7. 不重写或删除 `3_Triton/`、`4_CUDA/` 中的学习成果。
8. 阶段性完成时更新本文件中的实际状态，记录完成内容、测试结果和剩余限制。

## 新上下文启动提示词

在新上下文中发送下面这段话：

```text
我要开始 OperatorEngine 阶段 4.5 项目。请先完整阅读
E:/Code/C++Compiler/OperatorEngine/项目开发步骤.md，检查工作区中的
3_Triton 和 4_CUDA 现有实现，然后严格从Phase 0 和Phase 1 开始。

这个项目用于学习算子工程化。请以带我实现、解释接口和 review 代码为主，
不要一次性替我写完整项目。每次只推进一个可验证的小步骤，并在修改后运行
对应测试。当前第一目标是打通 PyTorch Dispatcher -> C++ wrapper -> current
CUDA stream -> Vector Add CUDA kernel -> pytest/opcheck 的完整闭环。
```

## 当前状态

- 阶段五：暂停。
- OperatorEngine：Phase 0 与 Phase 1 已完成；Vector Add 已打通 Dispatcher、C++ wrapper、current CUDA stream、CUDA kernel、pytest、opcheck 和 benchmark。
- 当前里程碑：Phase 5 已于 2026-09-18 验收完成。最小 Transformer Block 已覆盖纯 PyTorch baseline、Attention-only、GEMM-only、Softmax-only unfused 和 Attention + GEMM 组合版本，并完成同权重 correctness、方法路由测试和逐项端到端 benchmark。详细结论见 `docs/phase5_acceptance.md`，性能欠账继续统一延后处理。
- Phase 0 验证环境：WSL2 Ubuntu 20.04、PyTorch 2.11.0+cu128、CUDA Toolkit 12.8、RTX 5060 Laptop GPU（sm_120）。
- 当前完成状态：Phase 0 到 Phase 6 已于 2026-09-18 全部验收完成。editable 安装成功，全量测试为 `184 passed, 1 skipped`，一键核心 benchmark 已生成五份有效结果；详细记录见 `docs/phase6_acceptance.md`。
- 后续方向：可先处理已有性能欠账，再进入 Prefill/Decode、KV Cache 和 continuous batching；多卡推理继续暂停。
